{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://sonic-forge.local/schemas/voice-list-request.json",
  "title": "SonicForge voices",
  "type": "object",
  "properties": {},
  "additionalProperties": false,
  "description": "The character voices that exist, plus the built-in preset speakers. Each entry carries the emotions it can produce (emotion_choices) and whether emotion is chosen by name (supports_emotion). Pick the voice_id here before making any line.\n\nIt creates nothing — sonic.voice.create makes a voice, sonic.generate speaks with one."
}
